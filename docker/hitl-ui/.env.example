# HITL UI Environment Configuration
#
# Copy this file to .env.local for local development:
#   cp .env.example .env.local
#
# The .env.local file is gitignored and won't be committed.

# =============================================================================
# Backend Configuration
# =============================================================================

# Use mock data for development (no backend required)
# Set to 'true' to use mock data, 'false' to use real backend
VITE_USE_MOCKS=true

# Default search backend mode (mock, rest, graphql, mcp)
# Only used if VITE_USE_MOCKS is false
# Users can also toggle this via the BackendSelector in the UI
VITE_SEARCH_BACKEND=rest

# API base URL for REST/GraphQL backends
# In development, Vite proxies /api to this URL
# In production, this should be the full backend URL
VITE_API_BASE_URL=/api

# =============================================================================
# Development vs Production
# =============================================================================
#
# LOCAL DEVELOPMENT (mock mode):
#   1. Copy this file to .env.local
#   2. Keep VITE_USE_MOCKS=true
#   3. Run: npm run dev
#   4. Use BackendSelector to switch to 'Mock' mode
#
# LOCAL DEVELOPMENT (with backend):
#   1. Start the backend: python -m src.orchestrator.main
#   2. Start Elasticsearch: docker-compose up elasticsearch
#   3. Set VITE_USE_MOCKS=false in .env.local
#   4. Run: npm run dev
#   5. Use BackendSelector to switch to 'REST API' mode
#
# PRODUCTION:
#   1. Set VITE_USE_MOCKS=false
#   2. Set VITE_API_BASE_URL to the production backend URL
#   3. Build: npm run build
#   4. Deploy the dist/ folder

# =============================================================================
# Backend Modes
# =============================================================================
#
# The UI supports multiple backend modes via BackendSelector:
#
# - mock:    Local mock data (default for development)
# - rest:    HTTP REST API to /api/knowledge-store/*
# - graphql: GraphQL queries (deferred - not yet implemented)
# - mcp:     MCP tool calls (deferred - not yet implemented)
#
# Users can switch modes at runtime using the dropdown in the search page.
# The selection is persisted to localStorage.
