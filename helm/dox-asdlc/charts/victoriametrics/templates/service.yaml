{{- if .Values.enabled }}
apiVersion: v1
kind: Service
metadata:
  # Use "metrics-store" name for backend abstraction
  name: {{ include "victoriametrics.serviceName" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "victoriametrics.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "victoriametrics.selectorLabels" . | nindent 4 }}
{{- end }}
